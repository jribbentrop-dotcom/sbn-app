APP_NAME="Soul Bossa Nova"
APP_ENV=production
APP_KEY=                          # ← run: php artisan key:generate --show
APP_DEBUG=false
APP_URL=https://soulbossanova.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

LOG_CHANNEL=stack
LOG_LEVEL=error

DB_CONNECTION=sqlite
DB_DATABASE=/var/www/sbn-app/database/sbn.db

CACHE_DRIVER=file
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Mail — set SMTP values when ready; log for now
MAIL_MAILER=log
MAIL_FROM_ADDRESS=noreply@soulbossanova.com
MAIL_FROM_NAME="Soul Bossa Nova"

# Payments
PAYMENTS_PROVIDER=stripe_managed
STRIPE_SECRET=sk_live_...         # ← live secret key from Stripe dashboard
STRIPE_WEBHOOK_SECRET=whsec_...   # ← from Stripe webhook endpoint for soulbossanova.com
STRIPE_TAX_CODE=txcd_10302000     # ← or whatever you set during setup

# Reverb (broadcasting) — disable for now if not using real-time
REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

# OpenAI (used for transcription features)
OPENAI_API_KEY=

SHOP_USD_RATE=1.08
